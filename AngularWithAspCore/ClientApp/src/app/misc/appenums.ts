export enum Operator {
    Gt,
    Lt,
    Eq,
    Le,
    Ge,
    Ne,
    IsNull,
    IsNotNull,
    IsEmpty,
    IsNotEmpty,
    Contains,
    NotContains,
    StartsWith,
    EndsWith,
    In,
    NotIn
}

export enum SortOrder {
    Asc,
    Desc
}

export enum Logic {
    And,
    Or
}
